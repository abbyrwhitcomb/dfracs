% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap_capm.R
\name{bootstrap_capm}
\alias{bootstrap_capm}
\title{Bootstrap Standard Errors for CAPM Estimates}
\usage{
bootstrap_capm(stock, market, rf, B = 500)
}
\arguments{
\item{stock}{A numeric vector of monthly returns for a single stock.}

\item{market}{A numeric vector of monthly market returns (e.g., S&P 500).}

\item{rf}{A numeric vector of monthly risk-free rates (e.g., 3-month T-bill rates).}

\item{B}{Integer. Number of bootstrap replications (default is 500).}
}
\value{
A named numeric vector with bootstrap standard errors for:
\describe{
  \item{alpha_se}{Standard error of alpha (intercept from CAPM).}
  \item{beta_se}{Standard error of beta (slope from CAPM).}
  \item{sharpe_se}{Standard error of the Sharpe ratio.}
  \item{treynor_se}{Standard error of the Treynor ratio.}
}
}
\description{
This function uses a nonparametric bootstrap procedure to estimate the standard errors
of CAPM-related quantities: alpha, beta, Sharpe ratio, and Treynor ratio.
It resamples the data with replacement and fits the CAPM model in each bootstrap sample.
}
\examples{
for (i in 1:ncol(stocks)) {
 boot_est[i, 2:5] <- bootstrap_capm(stocks[, i], market, rf)}
print(boot_est)

}
